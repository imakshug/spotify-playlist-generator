{
  "name": "spotify-playlist-generator",
  "version": "1.0.0",
  "description": "Create Spotify playlists by pasting song names - automatically search and add tracks",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "spotify",
    "playlist",
    "music",
    "api",
    "react",
    "node"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
